/**
 * Dashboard.tsx — Punto de entrada del dashboard.
 * Re-exporta DashboardPage para mantener compatibilidad con el router.
 * El router importa "Dashboard" desde esta ruta y no necesita cambiar.
 */
export { default } from "./DashboardPage";
